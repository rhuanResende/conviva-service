package com.desenvolvimento.logica.conviva.conviva_service.auth.enums;

public enum SecurityEventType {
    LOGIN_SUCCESS,
    LOGIN_FAILED,
    ACCOUNT_LOCKED,
    PASSWORD_CHANGED,
    FORCE_PASSWORD_CHANGE,
    REFRESH_REUSE_DETECTED,
    REFRESH_EXPIRED,
    LOGOUT
}
